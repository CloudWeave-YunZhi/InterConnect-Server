version: '3.8'

services:
  interconnect-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8000
      - DATABASE_PATH=/data/minecraft_ws.db
    volumes:
      - ./data:/data
    restart: unless-stopped

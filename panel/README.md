# InterConnect 管理面板

基于 Vue 3 + Element Plus 的 InterConnect 服务器管理面板。

## 特性

- 🎨 **绿色/橙色主题** - 清新自然的配色方案
- 📱 **响应式设计** - 适配各种屏幕尺寸
- 🔒 **安全认证** - Bearer Token 认证机制
- 📊 **节点管理** - 创建、删除、踢下线节点
- 🔍 **搜索过滤** - 支持节点名称和 UUID 搜索
- 📄 **分页展示** - 大数据量友好展示

## 安装

```bash
cd admin
npm install
```

## 开发

```bash
npm run dev
```

默认运行在 http://localhost:5173

## 构建

```bash
npm run build
```

构建后的文件位于 `dist` 目录。

## 配置

### 代理配置

在 `vite.config.js` 中修改代理设置：

```javascript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',  // 后端地址
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')
    }
  }
}
```

### 环境变量

复制 `.env.example` 为 `.env` 并根据需要修改：

```bash
cp .env.example .env
```

## API 说明

| 接口 | 方法 | 说明 |
|------|------|------|
| `/login` | POST | 登录获取 Token |
| `/manager/keys` | GET | 获取节点列表 |
| `/manager/keys/:servername` | POST | 创建/重置节点 |
| `/manager/keys/:servername` | DELETE | 删除节点 |
| `/manager/kick/:servername` | POST | 踢下线节点 |

## 项目结构

```
admin/
├── src/
│   ├── api/           # API 接口
│   ├── components/    # 组件
│   ├── router/        # 路由配置
│   ├── styles/        # 样式文件
│   ├── views/         # 页面视图
│   ├── App.vue        # 根组件
│   └── main.js        # 入口文件
├── index.html         # HTML 模板
├── package.json       # 依赖配置
└── vite.config.js     # Vite 配置
```

## 主题色

| 颜色 | 色值 | 用途 |
|------|------|------|
| 主色 | `#27ae60` | 按钮、高亮、在线状态 |
| 辅助色 | `#e67e22` | 统计、强调 |
| 成功 | `#27ae60` | 成功状态 |
| 警告 | `#f39c12` | 警告状态 |
| 危险 | `#e74c3c` | 删除操作 |
